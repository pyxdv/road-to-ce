# วิธีเพิ่มชื่อของตนเองขึ้น Class Wall (Mission 3)

คู่มือนี้ทำผ่านหน้าเว็บ GitHub ทั้งหมด ไม่ต้องติดตั้งโปรแกรม ไม่ต้องใช้ command line
ใช้เวลาประมาณ 15 นาที

สิ่งที่จะได้เรียนรู้คือกระบวนการทำงานที่ใช้จริงในงานพัฒนาซอฟต์แวร์:
`branch → commit → pull request → review → merge → deploy`

พื้นฐานของแต่ละคำ อ่านได้ที่ใบความรู้ที่ 3.1 หัวข้อ 7

---

## ก่อนเริ่ม

- [ ] มีบัญชี GitHub ที่ยืนยันอีเมลแล้ว
- [ ] มีเว็บไซต์ของตนเองจาก Mission 1 ที่เปิดได้จริง (`https://<username>.github.io`)
- [ ] ผ่าน Mission 0 (Privacy Check) แล้ว

---

## ขั้นที่ 1 — สร้างไฟล์ข้อมูลของตนเอง

**ไม่ต้อง fork ด้วยตนเอง** GitHub จะสร้าง fork ให้อัตโนมัติเมื่อเริ่มสร้างไฟล์

1. เปิด repository `pyxdv/road-to-ce` แล้วเข้าโฟลเดอร์ `students/`
2. เปิดไฟล์ `_TEMPLATE.json` กดปุ่ม **Copy raw file** เพื่อคัดลอกเนื้อหา
3. กลับมาที่โฟลเดอร์ `students/` → **Add file** → **Create new file**
4. GitHub จะแจ้งว่าไม่มีสิทธิ์เขียนใน repository นี้ และจะสร้าง fork ให้ กดยืนยัน

   > **fork คืออะไร** คือสำเนาของ repository ทั้งชุดที่อยู่ในบัญชีของเรา
   > เราแก้ไขในสำเนาของเราได้เต็มที่โดยไม่กระทบต้นฉบับ แล้วจึงเสนอให้เจ้าของต้นฉบับ
   > พิจารณานำการแก้ไขของเราเข้าไป

5. ตั้งชื่อไฟล์เป็น **GitHub username ของตนเอง ตามด้วย `.json`**

   ```
   students/somchai-dev.json
   ```

   > ชื่อไฟล์ต้องตรงกับค่าในช่อง `"github"` ทุกตัวอักษร รวมถึงตัวพิมพ์เล็กพิมพ์ใหญ่

6. วางเนื้อหาที่คัดลอกมา แล้วแก้ให้เป็นข้อมูลของตนเอง

```json
{
  "github": "somchai-dev",
  "display_name": "สมชาย (ชาย)",
  "cohort": "ECP1N",
  "year": 2569,
  "tagline": "สนใจงานหุ่นยนต์และระบบฝังตัว",
  "site": "https://somchai-dev.github.io",
  "interests": ["embedded", "robotics", "network"],
  "accent": "#38bdf8"
}
```

| ช่อง | ใส่อะไร | บังคับ |
|---|---|---|
| `github` | GitHub username (ต้องตรงกับชื่อไฟล์) | ✅ |
| `display_name` | ชื่อที่ต้องการให้แสดงบนเว็บ **ใช้ชื่อเล่นได้** | ✅ |
| `cohort` | `ECP1N` หรือ `ECP2R` | ✅ |
| `year` | ปีการศึกษาที่เข้าเรียน เช่น `2569` | ✅ |
| `tagline` | หนึ่งประโยคว่าสนใจสร้างอะไร (ไม่เกิน 120 ตัวอักษร) | ✅ |
| `site` | URL เว็บไซต์ของตนเองจาก Mission 1 (ต้องขึ้นต้นด้วย `https://`) | ✅ |
| `interests` | หัวข้อที่สนใจ 1–5 รายการ | — |
| `accent` | สีประจำรายการ เป็นรหัสสี hex เช่น `#38bdf8` | — |

7. เลื่อนลงล่างสุด ช่อง **Commit message** พิมพ์ว่า `Add student card: somchai-dev`
8. กด **Propose new file**

> **ทำไมต้องแยก branch** branch คือสายการพัฒนาที่แยกออกมา ทำให้ทดลองแก้ไขได้
> โดยที่สายหลัก (`main`) ยังใช้งานได้ตามปกติ GitHub จะตั้งชื่อ branch ให้อัตโนมัติ
> เมื่อกด Propose new file

---

## ขั้นที่ 2 — เปิด Pull Request

1. GitHub จะพาไปยังหน้าเปิด Pull Request ให้ทันทีหลังกด Propose new file
2. ตรวจว่าทิศทางของ Pull Request ถูกต้อง

   ```
   base: pyxdv/road-to-ce  main   ←   compare: <username>/road-to-ce  <branch ของเรา>
   ```

3. ตั้งชื่อ Pull Request เป็น `Add student card: <username>`
4. กรอกแบบฟอร์มที่ปรากฏขึ้นให้ครบ โดยเฉพาะ URL เว็บไซต์และรายการตรวจสอบข้อมูลส่วนบุคคล
5. กด **Create pull request**

**สิ่งที่เกิดขึ้นทันที** ระบบตรวจอัตโนมัติจะเริ่มทำงาน ดูผลได้ที่ช่อง Checks ท้าย Pull Request

- ✅ เขียว คือผ่าน รอเพื่อนร่วมรุ่น review
- ❌ แดง คือไม่ผ่าน **กดเข้าไปอ่านรายงานว่าผิดที่ใด** แล้วแก้ไฟล์บน branch เดิม
  Pull Request จะอัปเดตและตรวจซ้ำให้เอง ไม่ต้องเปิด Pull Request ใหม่

> ผลตรวจสีแดงไม่ใช่ความล้มเหลว แต่เป็นการที่ระบบรายงานว่าพบอะไรผิดปกติและผิดที่ตรงไหน
> ให้อ่านรายงานก่อนลงมือแก้เสมอ

---

## ขั้นที่ 3 — ทำ code review ให้เพื่อน 1 คน

ไปที่แท็บ **Pull requests** ของ repository กลาง เลือก Pull Request ของเพื่อนที่ยังไม่มีใคร review

1. กดแท็บ **Files changed**
2. อ่านไฟล์ของเพื่อน แล้วตรวจ 3 ข้อ
   - [ ] เปิด URL ในช่อง `site` แล้วเว็บแสดงผลได้จริง ไม่ใช่ status code 404
   - [ ] ไม่มีข้อมูลส่วนบุคคลที่ไม่ควรเผยแพร่
   - [ ] `tagline` อ่านเข้าใจได้ และเหมาะกับพื้นที่สาธารณะ
3. กด **Review changes** ที่มุมขวาบน
4. เขียนข้อเสนอแนะ **อย่างน้อย 1 ประโยคที่มีเนื้อหา** ระบุสิ่งที่ทำได้ดี 1 อย่าง
   และสิ่งที่ควรปรับ 1 อย่าง

   > ข้อความที่ใช้ไม่ได้: "ดีครับ" · "ผ่าน" · "👍"
   > ข้อความที่ใช้ได้: "tagline เขียนได้ชัดเจนดี ส่วนหน้าเว็บแนะนำให้เปลี่ยนค่า accent
   > ให้ต่างจากค่าตั้งต้น จะได้แยกออกจากของคนอื่นได้ง่ายขึ้น"

5. เลือก **Approve** หากผ่าน หรือ **Request changes** หากพบปัญหา แล้วกด **Submit review**
6. จดหมายเลข Pull Request ที่ไป review ลงในใบงาน

---

## ขั้นที่ 4 — รอ merge

เมื่อผลตรวจอัตโนมัติผ่านและมีเพื่อน approve แล้ว ผู้สอนจะกด **Merge**
จากนั้นระบบจะทำงานต่อเองทั้งหมด

```
merge เข้า main
   ↓  (GitHub Actions เริ่มทำงานเอง)
รัน build_wall.py อ่านทุกไฟล์ใน students/
   ↓
สร้าง site/index.html ขึ้นใหม่
   ↓
deploy ขึ้น GitHub Pages
   ↓
ข้อมูลของเราปรากฏบน Class Wall
```

หลังกด merge แล้วไม่มีใครเข้าไปแตะขั้นตอนใดอีกเลย กระบวนการนี้เรียกว่า CI/CD
(ใบความรู้ที่ 3.1 หัวข้อ 8)

---

## ปัญหาที่พบบ่อย

| อาการ | สาเหตุที่พบบ่อย | วิธีแก้ |
|---|---|---|
| รายงานว่า `filename must match "github" field` | ชื่อไฟล์กับค่าในช่อง `github` ไม่ตรงกัน | แก้ให้ตรงกัน ตัวพิมพ์เล็กพิมพ์ใหญ่ต้องตรงด้วย |
| รายงานว่า `invalid JSON` | ขาดหรือเกินเครื่องหมายจุลภาค | อ่านเลขบรรทัดที่รายงานระบุ แล้วตรวจจุลภาครอบบรรทัดนั้น |
| รายงานว่า `possible personal data` | มีเบอร์โทรศัพท์ อีเมล หรือเลข 13 หลักอยู่ในไฟล์ | ลบออก ใช้ลิงก์ GitHub เป็นช่องทางติดต่อแทน |
| เปิด `site` แล้วได้ status code 404 | ยังไม่ได้เปิดใช้งาน GitHub Pages หรือระบบยังสร้างไม่เสร็จ | Settings → Pages เลือก branch `main` แล้วรอ 1–2 นาที |
| Pull Request ชี้ผิดทิศทาง | base เป็น repository ของตนเองแทนที่จะเป็น repository กลาง | ปิด Pull Request แล้วเปิดใหม่ ตรวจบรรทัด base/compare |
| ไม่พบปุ่ม Add file | ยังไม่ได้เข้าโฟลเดอร์ `students/` | ตรวจว่า URL ลงท้ายด้วย `/tree/main/students` |

---

## สรุปกระบวนการ

```
สร้างไฟล์ข้อมูล  →  GitHub สร้าง fork และ branch ให้อัตโนมัติ  →  pull request
                                              ↓
                                     ระบบตรวจอัตโนมัติ
                                              ↓
                                 เพื่อนร่วมรุ่น review + approve
                                              ↓
                                        ผู้สอน merge
                                              ↓
                                    CI สร้างหน้าเว็บ + deploy
                                              ↓
                                     ข้อมูลปรากฏบน Class Wall
```
