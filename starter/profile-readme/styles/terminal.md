<!-- ==========================================================================
     สไตล์ที่ 4 — Terminal / Systems  (แนว muesli)
     จัดหน้าให้มีกลิ่นอาย command line เหมาะกับคนที่จะไปทางระบบสมองกลฝังตัว
     เครือข่าย หรือ backend เพราะบอกรสนิยมการทำงานได้โดยไม่ต้องเขียนว่า "ผมถนัด..."

     ข้อจำกัดที่ต้องรู้ก่อนใช้
     - ข้อความที่อยู่ในบล็อกโค้ด กดลิงก์ไม่ได้ ค้นหาไม่เจอ และอ่านบนมือถือลำบากถ้าบรรทัดยาว
       จึงต้องมีลิงก์จริงเป็นข้อความปกติอยู่นอกบล็อกเสมอ (ทำไว้ให้แล้วด้านล่าง)
     - พิมพ์บรรทัดให้สั้นกว่า 60 ตัวอักษร ไม่งั้นบนมือถือจะเลื่อนซ้ายขวา
     - นี่คือ "ภาพจำลอง" ของ terminal ไม่ใช่ผลลัพธ์จริง อย่าเขียนอะไรที่ไม่จริงลงไป

     วิธีใช้ กด Copy raw file ที่มุมขวาบน แล้วนำไปวางใน README.md ของ repository
     ที่ชื่อเดียวกับ username ของตนเอง จากนั้นแก้ข้อความในวงเล็บเหลี่ยม [ ] แล้วลบวงเล็บออก
     ========================================================================== -->

```console
$ whoami
[username] — [ชื่อที่ใช้แสดง]

$ cat /etc/profile
role   : computer engineering freshman
school : RMUTI, Thailand
year   : 1 / semester 1
goal   : [สิ่งที่อยากสร้าง เขียนสั้น ๆ เป็นภาษาอังกฤษหรือไทยก็ได้]

$ ls interests/
embedded-systems/   networking/   web/

$ cat now.md
building : [งานที่ทำอยู่จริงในเดือนนี้]
learning : html, css, git, [เพิ่มเมื่อได้เริ่มจริง]
next     : [สิ่งที่ตั้งใจจะเริ่มต่อไป]

$ git log --oneline -3
[a1b2c3d] เข้าใจว่า DNS ทำงานก่อน browser ต่อไปหา server
[e4f5g6h] deploy เว็บหน้าแรกขึ้น GitHub Pages สำเร็จ
[i7j8k9l] เริ่มใช้ Git ครั้งแรก

$ _
```

<!-- ส่วนนี้ต้องอยู่นอกบล็อกโค้ด เพราะลิงก์ในบล็อกโค้ดกดไม่ได้ -->

**เว็บไซต์** [[username].github.io](https://[username].github.io) ·
**ติดต่อ** [GitHub](https://github.com/[username])

<!-- ช่องทางติดต่อ ใช้ลิงก์สาธารณะเท่านั้น ห้ามใส่เบอร์โทรศัพท์ อีเมลส่วนตัว หรือที่อยู่ -->
